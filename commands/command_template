import disnake
from disnake.ext import commands


class Name(commands.Cog):

    def __init__(self, client):
        self.client = client

    @commands.slash_command()
    async def name(self, inter):



def setup(bot):
    bot.add_cog(Name(bot))
